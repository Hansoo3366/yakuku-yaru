# 재사용 코드

다른 프로젝트에서도 가져다 쓸 만한 패턴 모음. 경로는 이 저장소 기준.

---

## 1. 공통 API 클라이언트 + ApiError

**위치**: `apps/web/src/lib/api.ts`

```typescript
export class ApiError extends Error {
  constructor(
    public readonly status: number,
    public readonly code: string,
    message: string,
  ) {
    super(message);
  }
}

export async function request<T>(path: string, options: RequestOptions = {}) {
  const response = await fetch(`${API_URL}${path}`, {
    method: options.method ?? 'GET',
    headers: { 'Content-Type': 'application/json', /* Authorization */ },
    body: options.body ? JSON.stringify(options.body) : undefined,
    credentials: 'include',
  });

  if (!response.ok) {
    const error = await response.json().catch(() => ({
      code: 'UNKNOWN_ERROR',
      message: '요청 처리 중 오류가 발생했습니다.',
    }));
    throw new ApiError(response.status, error.code, error.message);
  }

  if (response.status === 204) return undefined as T;
  const text = await response.text();
  return text ? (JSON.parse(text) as T) : (undefined as T);
}
```

**재사용 포인트**: `credentials: 'include'`, 204 처리, 백엔드 공통 에러 포맷 `{ code, message }` 연동.

---

## 2. `HttpOnly` 쿠키 JWT 인증

**위치**: `apps/api/src/modules/auth/auth-cookie.ts`

```typescript
export function setAuthCookie(res: Response, token: string, options = {}) {
  res.cookie(AUTH_COOKIE_NAME, token, {
    httpOnly: true,
    maxAge: options.rememberMe ? getCookieMaxAgeMs(env.jwt.rememberExpiresIn) : undefined,
    sameSite: 'lax',
    secure: env.nodeEnv === 'production',
  });
}
```

**재사용 포인트**: XSS로 토큰 탈취 방지. 프론트는 JS로 토큰을 읽지 않고 `credentials: 'include'`만 사용.

---

## 3. 인메모리 Rate Limit 미들웨어

**위치**: `apps/api/src/middleware/rate-limit.ts`

```typescript
export function rateLimit(options: RateLimitOptions): RequestHandler {
  return (req, res, next) => {
    const key = `${options.scope}:${req.user ? `user:${req.user.id}` : `ip:${req.ip}`}`;
    // sliding window bucket → 429 + Retry-After
  };
}
```

**재사용 포인트**: Redis 없이 로그인·회원가입·글쓰기 남용 방지. 단일 인스턴스에 적합.

---

## 4. 클라이언트 사이드 WebP 리사이즈

**위치**: `apps/web/src/lib/attendance-api.ts` (`optimizeAttendancePhoto`)

```typescript
const bitmap = await createImageBitmap(file);
const scale = Math.min(1, TARGET_SIZE / Math.max(bitmap.width, bitmap.height));
// canvas draw → toBlob('image/webp', 0.84) → File
```

**재사용 포인트**: 업로드 전 용량·해상도 제한. HEIC 등도 `createImageBitmap`으로 처리 가능.

---

## 5. XSS 방지 입력 정제

**위치**: `apps/web/src/lib/user-input.ts`

```typescript
const BLOCKED_INPUT_PATTERNS = [
  /<\/?[a-z][^>]*>/i, /<script\b/i, /javascript:/i, /on\w+\s*=/i, ...
];

export function sanitizePlainText(value: string, maxLength: number) {
  return stripControlCharacters(value.replace(/<[^>]*>/g, ''))
    .trim()
    .slice(0, maxLength);
}
```

**재사용 포인트**: 게시판·닉네임 등 plain text 필드 이중 검증 (클라이언트 + 서버).

---

## 6. bcrypt 비밀번호 유틸

**위치**: `apps/api/src/utils/password.ts`

```typescript
const SALT_ROUNDS = 12;
export function hashPassword(password: string) {
  return bcrypt.hash(password, SALT_ROUNDS);
}
```

---

## 7. TanStack Query 공통 훅 모음

**위치**: `apps/web/src/lib/queries.ts`

여러 컴포넌트가 같은 API를 중복 호출하지 않도록 `useGamesQuery`, `useMeQuery` 등을 한 파일에 모음.

**재사용 포인트**: queryKey 규칙 + staleTime을 도메인별로 통일.

---

## 8. KBO 경기 upsert 키 전략

**위치**: `apps/api/src/modules/kbo-schedule/` (파싱·repository)

- `external_id` (KBO gameId) 우선
- 없으면 `pending-...` 임시 ID
- fallback unique: `(game_date, home_team_id, away_team_id)`

**재사용 포인트**: 외부 API ID가 늦게 붙는 일정 데이터 동기화.

---

## 9. GitHub Actions — VM에서 빌드하지 않기

**위치**: `.github/workflows/deploy.yml`

- Actions runner에서 Docker 이미지 빌드 → GHCR push
- VM은 `docker compose pull && up -d --no-build`

**재사용 포인트**: 소형 VM(e2-small)에서 Next.js 빌드 OOM 방지.

---

## 10. PWA — dev에서 SW 비등록

**위치**: `apps/web/src/components/ServiceWorkerRegister.tsx`

production에서만 service worker 등록 → 개발 중 캐시 꼬임 방지.

---

## 태그

#snippet #auth #pwa #rate-limit #image #kbo
