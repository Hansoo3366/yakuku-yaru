# Work Order

이 문서는 야크크 야르~ 섹시야구를 실제로 구현할 때의 추천 작업 순서입니다.

우선순위는 과제 필수 요구사항을 먼저 만족하고, 그 위에 야구 직관 앱 기능을 얹는 방식입니다.

## 0. Repository Setup

목표: 프로젝트 뼈대를 만든다.

작업:

- [x] 디자인 시스템 문서 작성
- [x] Git repository 초기화
- [x] Next.js 앱 생성
- [x] Express API 앱 생성
- [x] TypeScript 기본 설정
- [x] ESLint/Prettier 설정
- [x] `.env.example` 작성
- [x] 기본 README 실행 방법 업데이트

추천 완료 기준:

- [ ] `npm run dev` 또는 각 앱 dev 명령으로 프론트/백엔드가 실행된다.
- [ ] 브라우저에서 Next.js 첫 화면이 보인다.
- [x] API health check가 응답한다.

## 1. Docker and MySQL

목표: MySQL을 로컬에서 안정적으로 실행한다.

작업:

- [x] `docker-compose.yml` 작성
- [x] MySQL 컨테이너 설정
- [x] API 서버에서 MySQL 연결 확인
- [x] DB migration 또는 schema 초기화 방식 결정
- [x] `teams` seed 데이터 준비

추천 완료 기준:

- [x] `docker compose up -d`로 MySQL이 실행된다.
- [x] API 서버에서 DB 연결 성공 로그가 나온다.
- [x] 팀 목록 seed 데이터가 들어간다.

## 2. Backend Auth First

목표: 과제의 핵심인 로그인 흐름을 먼저 만든다.

작업:

- [x] `users` 테이블 생성
- [x] 회원가입 API
- [x] 이메일 중복 검사
- [x] bcrypt 비밀번호 암호화
- [x] 로그인 API
- [x] JWT 발급
- [x] 인증 미들웨어
- [x] `/auth/me` API
- [x] Swagger 기본 문서화

추천 완료 기준:

- [x] 회원가입 가능.
- [x] 로그인 시 JWT 발급.
- [x] JWT로 보호된 API 호출 가능.
- [x] Swagger에서 Auth API 확인 가능.

## 3. Frontend Auth

목표: 브라우저에서 로그인 상태를 유지한다.

작업:

- [x] 로그인 페이지
- [x] 회원가입 페이지
- [x] API client 작성
- [x] 로그인 토큰 저장 방식 결정
- [x] `/auth/me`로 로그인 상태 복원
- [x] 인증 필요 페이지 보호
- [x] 공통 에러 처리 기본 구조

추천 완료 기준:

- [ ] 브라우저에서 회원가입/로그인 가능
- [ ] 새로고침 후 로그인 상태가 유지된다.
- [ ] 비로그인 사용자가 보호 페이지 접근 시 로그인 화면으로 이동한다.

## 4. Required Board and Comments

목표: 과제 필수 CRUD를 먼저 끝낸다.

작업:

- [x] `posts` 테이블 생성
- [x] `comments` 테이블 생성
- [x] 게시글 작성/목록/상세/수정/삭제 API
- [x] 게시글 목록 페이징
- [x] 댓글 작성/조회/삭제 API
- [x] 작성자 권한 검증
- [x] 게시판 화면 구현
- [x] 댓글 UI 구현

추천 완료 기준:

- [x] 로그인 사용자가 글을 작성할 수 있다.
- [x] 본인 글만 수정/삭제할 수 있다.
- [x] 댓글 작성/조회/삭제가 가능하다.
- [x] 게시글 목록 페이징이 동작한다.

## 5. Baseball Domain Foundation

목표: 야구 앱의 기본 데이터 구조를 만든다.

작업:

- [x] `teams` 테이블 확정
- [x] `games` 테이블 생성
- [x] 경기 일정 seed 데이터 작성
- [x] 팀 목록 API
- [x] 내 팀 설정 API
- [x] 경기 일정 조회 API
- [x] 경기 상세 API

추천 완료 기준:

- [x] 사용자가 내 팀을 설정할 수 있다.
- [x] 내 팀 경기 일정만 조회할 수 있다.
- [x] 경기 상세에서 구장, 시간, 예매처, 예매 오픈 시간을 볼 수 있다.

## 6. Calendar UI

목표: 앱의 핵심 화면인 캘린더를 만든다.

작업:

- [x] 월간 캘린더 UI
- [x] 내 팀 경기 일정 표시
- [x] 전체 경기 보기 필터
- [x] 직관/집관 기록 필터
- [x] 기록 있는 날짜만 보기 필터
- [x] 경기 클릭 시 상세 이동
- [x] 모바일 하단 네비게이션 초안
- [x] 월 이동 기능
- [x] 캘린더 경기 카드 스코어 표시
- [x] 경기 취소 사유별 표시 준비
- [x] 선발/예상 선발 투수 표시 필드 준비

추천 완료 기준:

- [x] `/calendar`에서 월간 경기 일정이 보인다.
- [x] 내 팀을 바꾸면 캘린더 일정이 바뀐다.
- [x] 모바일 화면에서도 주요 흐름이 사용 가능하다.

## 6-1. KBO Detail Data

목표: KBO 경기 데이터의 표현력을 높인다.

작업:

- [x] `games.cancellation_reason` 추가
- [x] 취소 사유 분류: `rain`, `dust`, `ground`, `heat`, `cold`, `other`
- [x] 선수 마스터 테이블 초안 생성
- [x] 경기별 선발 투수 테이블 초안 생성
- [x] 경기별 라인업 테이블 초안 생성
- [x] KBO 선수 조회 페이지 크롤러 구현
- [x] KBO 선수 DB 적재 스크립트 구현
- [x] KBO 게임센터 선발 투수 동기화 구현
- [x] KBO 게임센터 선발 투수 스탯 동기화 구현
- [x] KBO 게임센터 라인업 동기화 구현
- [x] 경기 상세 선발 투수 프로필/스탯 UI 구현
- [x] 경기 상세 라인업 UI 구현
- [ ] 키플레이어 데이터 구조 확정

추천 완료 기준:

- [x] KBO 선수 목록을 DB에 적재할 수 있다.
- [x] 경기별 선발 투수를 캘린더와 경기 상세에서 볼 수 있다.
- [x] 경기 상세에서 선발 투수 평균자책점, WHIP, WAR, QS를 볼 수 있다.
- [x] 경기 상세에서 양 팀 라인업을 볼 수 있다.
- [x] 경기 취소 사유가 캘린더에 아이콘과 함께 표시된다.

## 7. Attendance Records and Photo Upload

목표: 직관 인증 기능을 구현한다.

작업:

- [x] `attendance_records` 테이블 생성
- [x] 직관 기록 작성/수정/삭제 API
- [x] 사진 업로드 API
- [x] 사진 저장 경로 정책 정리
- [x] 직관 기록 작성/수정 화면
- [x] 캘린더 사진 썸네일 표시

추천 완료 기준:

- [x] 경기별 직관 기록을 만들 수 있다.
- [x] 사진을 업로드할 수 있다.
- [x] 기록이 있는 날짜에 캘린더 썸네일이 보인다.
- [x] 사용자가 기록을 수정/삭제할 수 있다.

## 8. Score and Winning Fairy

목표: 앱의 재미 요소를 완성한다.

작업:

- [x] 경기 기본 스코어 표시
- [x] 직관 기록에서 스코어 수정 가능 처리
- [x] 승/패/무 계산
- [x] 내 직관 통계 API
- [x] 직관/집관 통계 분리
- [x] 마이페이지 통계 UI
- [x] 승률 50% 이상 `승리요정` 타이틀 표시

추천 완료 기준:

- [x] 마이페이지에서 직관 횟수, 승/패/무, 승률이 보인다.
- [x] 승률이 50% 이상이면 `승리요정` 타이틀이 보인다.

## 8-1. Product Polish

목표: 실제 앱처럼 쓰기 위한 기본 UX를 보강한다.

작업:

- [x] GNB와 Footer 추가
- [x] 회원가입 시 내 팀 선택
- [x] 마이페이지에서 내 팀 변경
- [x] 팀 로고 표시
- [x] 선택한 팀 기준 컬러 테마 적용
- [x] 경기 알림 설정/해제
- [x] 구장별 맛집/주차 정보 표시
- [x] 일정 동기화 전 사용할 가짜 경기 데이터 보강
- [x] 동행자 태그 API
- [x] 동행자 검색/선택 UI
- [x] 태그 알림 UI
- [x] 태그된 기록 캘린더 표시

추천 완료 기준:

- [x] 캘린더, 경기 상세, 마이페이지에서 팀 로고와 팀 컬러가 보인다.
- [x] 경기 상세에서 알림과 구장 정보를 확인할 수 있다.
- [x] 일정 동기화 없이도 seed 데이터로 기능을 테스트할 수 있다.
- [x] 다른 회원을 동행자로 태그하면 알림과 캘린더에 반영된다.

## 8-2. Companion Tag Approval

목표: 동행 태그를 받은 사용자가 직접 수락/거절할 수 있게 하고, 캘린더 표시를 응답 결과에 맞춰 분리한다.

작업:

- [x] `attendance_companions.status` 기본값을 `pending`으로 변경
- [x] 기존 데이터 마이그레이션 (`tagged` → `pending`)
- [x] `responded_at` 컬럼 추가
- [x] 새 row만 `pending` 으로 추가하고 기존 응답 상태는 보존하는 INSERT 정책
- [x] 캘린더에 `accepted` 상태만 노출하도록 viewer JOIN 필터링
- [x] 동행 태그 수락/거절 API (`PATCH /attendance-records/:recordId/companions/me`)
- [x] 동행자 응답 시 호스트에게 결과 알림 발송 (`companion_accepted` / `companion_rejected`)
- [x] 마이페이지 알림에 수락/거절 버튼 UI
- [x] 직관 기록 수정 화면에 동행자별 응답 상태 표시
- [x] OpenAPI / `api-spec.md` / `product-spec.md` 동기화

추천 완료 기준:

- [x] 동행 태그를 받으면 마이페이지 알림에서 직접 수락/거절을 선택할 수 있다.
- [x] 수락 전 태그는 캘린더에 표시되지 않는다.
- [x] 수락 이후에만 동행자의 캘린더에 `동행` 배지가 노출된다.
- [x] 호스트는 동행자별 응답 상태(`대기`/`수락`/`거절`)를 직관 기록 수정 화면에서 확인할 수 있다.
- [x] 동행자가 응답하면 호스트도 결과 알림을 받는다.

## 9. PWA

목표: 모바일에서 앱처럼 설치할 수 있게 만든다.

작업:

- [x] manifest 설정
- [x] 앱 아이콘 추가
- [x] theme color 설정
- [x] standalone display 설정
- [x] service worker 설정
- [x] offline fallback 페이지

추천 완료 기준:

- [ ] 모바일 브라우저에서 홈 화면 추가가 가능하다.
- [ ] 홈 화면에서 실행했을 때 앱처럼 열린다.

## 10. Deployment and Submission

목표: 제출 가능한 상태로 정리한다.

작업:

- [x] Docker production 설정
- [x] Cloud Server 배포
- [x] Swagger URL 확인
- [x] GitHub repository 정리
- [x] GitHub Actions 자동 배포
- [x] README에 실행/검증 URL 정리
- [x] Docker 배포 문서 정리
- [x] 평가 대비 설명 문서 정리

추천 완료 기준:

- [x] 제출 가능한 GitHub Repository가 있다.
- [x] 배포 URL이 있다.
- [x] Swagger API 문서 URL이 있다.

## 11. Admin and Operations

목표: 운영자가 사용자, 게시글, 핵심 데이터를 관리할 수 있는 최소 관리자 화면을 만든다.

작업:

- [x] 관리자 권한 모델 설계 (`users.role`)
- [x] 관리자 로그인/접근 제어
- [x] 사용자 목록/검색/역할 관리
- [x] 게시글/댓글 목록 조회 및 삭제 관리
- [x] 경기 일정/스코어/구장 정보 관리
- [ ] 업로드 이미지 용량 제한 및 최적화 정책 문서화
- [x] 관리자 페이지 UI (`/admin`) 구현
- [ ] 관리자 API OpenAPI 문서화

추천 완료 기준:

- [x] 일반 사용자는 `/admin`에 접근할 수 없다.
- [x] 관리자는 사용자와 게시글 데이터를 검색하고 필요한 조치를 할 수 있다.
- [x] 경기 일정 seed에 의존하지 않고 관리자 화면에서 주요 데이터를 관리할 수 있다.

## Recommended First Sprint

첫 번째 스프린트는 여기까지만 목표로 잡는 것을 추천합니다.

1. Repository setup
2. Docker and MySQL
3. Backend auth
4. Frontend auth

이 네 가지가 끝나면 과제의 핵심 흐름인 `프론트엔드 -> 백엔드 -> DB -> 인증`이 한 번 연결됩니다. 이후 게시판과 야구 기능을 붙이는 일이 훨씬 쉬워집니다.
